{
  "description": "Blocks PR create/ready/merge (gh, gh api, GraphQL, MCP) without an APPROVED /debate record for the exact commit. Does nothing in repositories without .claude/debate.json.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|mcp__.*",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/require-debate.mjs\"",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
